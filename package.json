{
  "name": "skribble",
  "version": "1.0.0",
  "description": "Drawing app with guessing in real-time",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "npm install && npm install --prefix client && npm run build --prefix client"
  },
  "author": "Krystian",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "zustand": "^4.5.1"
  }
}
